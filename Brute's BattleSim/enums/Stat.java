package enums;

/** The maximum/base stats of a unit. Note that for some stats, like hp, the current value is described as a separate primitive. */
public enum Stat {
	DMG, MAXHP, SPD, ARM, RNG, CURRENTHP, COST;
}